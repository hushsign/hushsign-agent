# dist

HushSign agent deliverables (P7-S7):

- multi-arch container image (amd64/arm64), cosign-signed (key-based)
- systemd unit + install script for non-container sites
- example `agent.yaml` + `policy.yaml`
